mutation CreateCompany(
  $title: String!
  $detail: String!
  $link: String!
  $linkType: CompanyLinkType!
  $imageURL: String!
) {
  createCompany(
    input: {
      title: $title
      detail: $detail
      link: $link
      linkType: $linkType
      imageURL: $imageURL
    }
  )
}
